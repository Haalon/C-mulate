# C-mulate
Cellular automaton playground
# Dependencies
* Cimg
# Build
```bash
g++ main.cpp -L/usr/X11R6/lib -lm -lpthread -lX11 -fopenmp
```
## windows
To run it on windows, I use cygwin with
```bash
startx
```
